// TEST: Safety Error - Conditional initialization
// EXPECTED WITH -s FLAG: Safety check failed
// Variable 'result' might not be initialized on all paths

def main with input x output y as
  if x < 0 then
    result = 1
  else
    skip;
  y = result
