// TEST: Safety OK - All variables initialized
// EXPECTED WITH -s FLAG: Safety check passed
// All variables properly initialized before use

def main with input x output y as
  z = 0;
  sum = 0;
  while x < 10 do (
    sum = sum + x;
    x = x + 1
  );
  y = sum + z
