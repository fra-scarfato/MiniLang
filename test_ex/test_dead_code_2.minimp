// TEST: Dead code in branches
// EXPECTED WITH -O FLAG: May optimize dead stores in branches

def main with input x output y as
  if x < 0 then (
    temp = 10;
    unused = 999
  ) else (
    temp = 20;
    unused = 888
  );
  y = temp
